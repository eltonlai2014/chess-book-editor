# Build-only tooling for producing the standalone server .exe (PyInstaller).
# Not needed to RUN from source — install only when packaging:
#   .\.venv\Scripts\pip.exe install -r requirements-build.txt
#   .\package.ps1        (or: .\.venv\Scripts\pyinstaller.exe server.spec)
pyinstaller>=6.10
pyinstaller-hooks-contrib>=2024.9
