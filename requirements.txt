# Pinned dependencies for the XQF editor backend.
#
# cchess is pinned to an EXACT commit, not @master: the editor's
# PatchedXQFWriter and FEN handling are validated against this revision, and
# upstream master can change XQF/FEN behaviour without notice (see CLAUDE.md
# "Don't pip install --upgrade cchess"). Bump this only after re-running
# tests\test_roundtrip.py.
cchess @ git+https://github.com/walker8088/cchess.git@046351f738992e86224860520cf810070c772c21

Flask==3.1.3
Werkzeug==3.1.8
Jinja2==3.1.6
MarkupSafe==3.0.3
click==8.4.1
blinker==1.9.0
itsdangerous==2.2.0
colorama==0.4.6
chardet==7.4.3
